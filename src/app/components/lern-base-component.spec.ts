
import {LernBaseComponent} from './lern-base-component';

describe('LernBaseComponent', () => {
    it('should create an instance', () => {
        expect(new LernBaseComponent()).toBeTruthy();
    });
});
